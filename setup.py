from setuptools import setup,find_packages
setup(
    name="pysim",
    version="0.5",
    description="A python lib for dynamic system simulator, "+
    "including *Transfer Function*,*State Space Function* and "+
    "simple *Dynamic system Gragh* modules "+
    "and other essential utils",
    packages=find_packages()
)